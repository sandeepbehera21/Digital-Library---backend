spring.application.name=Digital-Library



# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/Digital_library
spring.datasource.username=root
spring.datasource.password=Sandeep#933
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA settings
spring.jpa.hibernate.ddl-auto=update   
spring.jpa.show-sql=true                
spring.jpa.properties.hibernate.format_sql=true

# Server configuration (optional)
server.port=8080


spring.jpa.open-in-view=false

# Redis configuration for caching
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.repositories.enabled=false

# Switch cache to Redis
spring.cache.type=redis

# App cache settings
app.cache.ttl-seconds=600

# JWT configuration (override via environment in production)
# 256-bit base64 secret example below. Replace for production.
app.jwt.secret=VGhpc0lzQVRlc3RTZWNyZXRLZXlGb3JKV1RDb25maWcwMDAxMjM0NTY3ODkwMTIzNA==
app.jwt.access-token-ttl-ms=900000
app.jwt.refresh-token-ttl-ms=604800000

